<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testproject">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestProject">
        <activity android:name=".CurrentPeriodSentenceActivity"></activity>
        <activity android:name=".PeriodSentenceActivity" />
        <activity android:name=".AddSentenceActivity" />
        <activity android:name=".CurrentSentenceActivity" />
        <activity android:name=".SentenceActivity" />
        <activity android:name=".AddCriminal_caseActivity" />
        <activity android:name=".CurrentCriminal_caseActivity" />
        <activity android:name=".Criminal_caseActivity" />
        <activity android:name=".AddAccusedActivity" />
        <activity android:name=".CurrentAccusedActivity" />
        <activity android:name=".AccusedActivity" />
        <activity android:name=".CurrentOrganEmployeeActivity" />
        <activity android:name=".Organ_EmployeeActivity" />
        <activity android:name=".AddStatementActivity" />
        <activity android:name=".AddVictimActivity" />
        <activity android:name=".CurrentStatementActivity" />
        <activity android:name=".StatementActivity" />
        <activity android:name=".CurrentVictimActivity" />
        <activity android:name=".VictimActivity" />
        <activity android:name=".MenuActivity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>